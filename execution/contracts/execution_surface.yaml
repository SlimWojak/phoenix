# Execution Surface Contract — S54 (updated from S28.C)
# VERSION: 2.0
# STATUS: PAPER + IBKR_PAPER (live-ready)

contract_id: EXEC-SURFACE-001
version: "2.0.0"
sprint: "S54"
status: ACTIVE

# =============================================================================
# MODULE IDENTITY
# =============================================================================

module:
  id: phoenix.execution
  name: Execution Path (Constitutional)
  tier: T0/T1/T2_PAPER  # T2 with paper broker, constitutional governance
  mode: SHADOW_PAPER
  capital: PAPER_ONLY

# =============================================================================
# S28.C CAPABILITIES
# =============================================================================

capabilities:
  intent_validation:
    schema_enforcement: true
    deterministic_hash: true
    halt_gate_integration: true

  position_lifecycle:
    states: [PROPOSED, APPROVED, SUBMITTED, STALLED, FILLED, MANAGED, CLOSED, CANCELLED, REJECTED, EXPIRED]
    valid_transitions: enforced
    state_machine: deterministic
    canonical_source: execution/positions/states.py

  paper_broker:
    fill_mode: IMMEDIATE
    slippage: NONE  # v0 simplified
    fees: NONE      # v0 simplified
    pnl_calculation: SIMPLIFIED_V0

  replay_harness:
    deterministic: PROVEN
    halt_integration: true
    mock_signals: SYNTHETIC_ONLY

# =============================================================================
# PERMISSIONS
# =============================================================================

permissions:
  reads:
    - cso.beads.approved
    - governance.halt_signal
    - market_data.prices

  writes:
    - execution.intents
    - execution.positions
    - execution.orders_paper
    - execution.blocked_log

  forbidden:
    - broker.live
    - orders.live
    - capital.real
    - submit_order_live
    - execute_order_live

# =============================================================================
# INVARIANTS
# =============================================================================

invariants:
  - id: INV-GOV-HALT-BEFORE-ACTION
    description: "Halt check precedes any capital-affecting action"
    enforcement: HaltGateViolation exception
    test: test_halt_before_exec.py
    status: PROVEN

  - id: INV-CONTRACT-1
    description: "State machine is deterministic"
    enforcement: hash comparison
    test: test_execution_path.py::TestGateC2Deterministic
    status: PROVEN

  - id: INV-EXEC-LIFECYCLE-1
    description: "Only valid state transitions allowed"
    enforcement: InvalidTransitionError exception
    test: test_execution_path.py::TestGateC4LifecycleValid
    status: PROVEN

# =============================================================================
# POSITION STATE MACHINE
# =============================================================================

position_lifecycle:
  version: 2
  canonical_source: execution/positions/states.py
  states:
    PROPOSED:
      description: "Intent received, awaiting T2 approval"
      transitions_to: [APPROVED, CANCELLED]
    APPROVED:
      description: "Human approved, T2 token issued"
      transitions_to: [SUBMITTED, EXPIRED]
    SUBMITTED:
      description: "Order sent to broker, awaiting fill"
      transitions_to: [FILLED, STALLED, REJECTED]
    STALLED:
      description: "Broker timeout (60s without ACK) — WP_C1"
      transitions_to: [FILLED, CANCELLED]
    FILLED:
      description: "Entry executed, position open"
      transitions_to: [MANAGED]
    MANAGED:
      description: "SL/TP in place, monitoring"
      transitions_to: [CLOSED]
    CLOSED:
      description: "Position exited (win/loss/BE)"
      transitions_to: []  # Terminal
    CANCELLED:
      description: "Order cancelled before fill"
      transitions_to: []  # Terminal
    REJECTED:
      description: "Broker rejected order"
      transitions_to: []  # Terminal
    EXPIRED:
      description: "T2 token expired before submission"
      transitions_to: []  # Terminal
  terminal_states: [CLOSED, CANCELLED, REJECTED, EXPIRED]
  invariants:
    - INV-POSITION-SM-1
    - INV-EXEC-LIFECYCLE-1

# =============================================================================
# P&L SEMANTICS (v0 SIMPLIFIED)
# =============================================================================

pnl_v0:
  formula: "(exit_price - entry_price) * size * direction_mult"
  direction_multiplier:
    LONG: +1
    SHORT: -1
  exclusions:
    - fees: "NOT INCLUDED (v0)"
    - slippage: "NOT INCLUDED (v0)"
    - commission: "NOT INCLUDED (v0)"
    - swap: "NOT INCLUDED (v0)"
  note: "GPT_LINT L28-C1: documented as simplified P&L v0"

# =============================================================================
# INTENT SCHEMA
# =============================================================================

intent_schema:
  required_fields:
    - intent_id: string
    - intent_type: enum[ENTRY, EXIT, SCALE, HEDGE, CANCEL]
    - status: enum[PENDING, BLOCKED, APPROVED, REJECTED, EXPIRED]
    - created_at: datetime
    - symbol: string
    - direction: enum[LONG, SHORT]
    - size: float
    - source_state_hash: string
    - intent_hash: string

  optional_fields:
    - expires_at: datetime
    - entry_price: float
    - stop_loss: float
    - take_profit: float
    - source_bead_id: string

# =============================================================================
# HALT GATE
# =============================================================================

halt_gate:
  pattern: "halt-first"
  enforcement: "HaltGate.check_before(action)"
  latency: "<50ms (INV-HALT-1)"

  flow:
    1_receive_intent: "Intent arrives"
    2_check_halt: "MUST check halt_signal"
    3_if_halted: "Block intent, raise BrokerHaltedError"
    4_if_clear: "Process order via paper broker"

  violations:
    - skip_check: "HaltGateViolation raised"
    - execute_while_halted: "BrokerHaltedError raised"
    - invalid_transition: "InvalidTransitionError raised"

# =============================================================================
# EXIT GATES (S28.C)
# =============================================================================

exit_gates:
  GATE_C1_PAPER_TRADES:
    test: test_execution_path.py::TestGateC1PaperTrades
    criterion: "signal → order → position → P&L cycle complete"
    status: PASS

  GATE_C2_DETERMINISTIC:
    test: test_execution_path.py::TestGateC2Deterministic
    criterion: "same replay = same result (hash match)"
    status: PASS

  GATE_C3_HALT_RESPECTED:
    test: test_execution_path.py::TestGateC3HaltRespected
    criterion: "halt stops all execution"
    status: PASS

  GATE_C4_LIFECYCLE_VALID:
    test: test_execution_path.py::TestGateC4LifecycleValid
    criterion: "only valid state transitions"
    status: PASS

# =============================================================================
# MOCK SIGNALS
# =============================================================================

mock_signals:
  mode: SYNTHETIC_ONLY
  source: MockSignalGenerator
  patterns:
    - FIXED: "signals at fixed intervals"
    - ALTERNATING: "alternate long/short"
    - FROM_PRICES: "momentum from price data"
  note: "NOT Olya methodology — testing only"

# =============================================================================
# CONSTRAINTS
# =============================================================================

constraints:
  capital: "PAPER_ONLY — no real money"
  signals: "MOCK — synthetic patterns only"
  broker: "STUB — immediate fills, no market simulation"
  pnl: "SIMPLIFIED_V0 — no fees/slippage"

# =============================================================================
# CHANGELOG
# =============================================================================

changelog:
  - version: "2.0.0"
    date: "2026-02-24"
    sprint: "S54"
    changes:
      - "Updated position lifecycle: 5-state S28 → 10-state canonical (execution/positions/states.py)"
      - "Added T2 approval flow: PROPOSED→APPROVED→SUBMITTED"
      - "Added problem states: STALLED, REJECTED, EXPIRED"
      - "Terminal states: CLOSED, CANCELLED, REJECTED, EXPIRED"
      - "Sourced from oracle audit TIER_2 finding D-1"

  - version: "0.2.0"
    date: "2026-01-23"
    sprint: "S28.C"
    changes:
      - Added position lifecycle state machine
      - Added paper broker stub
      - Added replay harness
      - Implemented P&L v0 (simplified)
      - 23/23 tests passing
      - All exit gates PASS

  - version: "0.1.0"
    date: "2026-01-22"
    sprint: "S27.0"
    changes:
      - Initial skeleton
      - Halt-first wiring
      - Intent objects only
      - No capital actions
